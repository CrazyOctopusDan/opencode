# Fork Windows desktop and standalone CLI packaging workflow.

name: Build Desktop and CLI (Windows)

on:
  push:
    branches: [dev-sapphire]
  workflow_dispatch:

concurrency:
  group: build-desktop-windows-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  version:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      version: ${{ steps.version.outputs.version }}
    steps:
      - uses: actions/checkout@v4

      - id: version
        run: |
          V=$(node -p "require('./packages/opencode/package.json').version")
          echo "version=$V" >> "$GITHUB_OUTPUT"

  build-cli-windows:
    needs: version
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-tags: true

      - uses: ./.github/actions/setup-bun

      # Reuse upstream's cross-platform CLI build, including the embedded Web UI.
      # OPENCODE_RELEASE stays unset: archives are uploaded here, never published.
      - name: Build CLI
        run: bun ./packages/opencode/script/build.ts
        env:
          OPENCODE_VERSION: ${{ needs.version.outputs.version }}
          OPENCODE_CHANNEL: prod

      - name: Package CLI
        shell: bash
        working-directory: packages/opencode
        run: |
          for target in opencode-windows-arm64 opencode-windows-x64 opencode-windows-x64-baseline; do
            test -s "dist/$target/bin/opencode.exe"
            (cd "dist/$target/bin" && zip -r "../../$target.zip" .)
          done

      - uses: actions/upload-artifact@v4
        with:
          name: opencode-cli-windows
          path: packages/opencode/dist/opencode-windows*.zip
          if-no-files-found: error

  build-desktop-windows:
    needs: version
    strategy:
      fail-fast: false
      matrix:
        settings:
          - host: windows-2025
            target: aarch64-pc-windows-msvc
            platform_flag: --win --arm64
            artifact: opencode-desktop-windows-arm64
          - host: windows-latest
            target: x86_64-pc-windows-msvc
            platform_flag: --win
            artifact: opencode-desktop-windows-x64
    runs-on: ${{ matrix.settings.host }}
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-tags: true

      - uses: ./.github/actions/setup-bun

      - uses: actions/setup-node@v4
        with:
          node-version: "24"

      - name: Prepare
        run: bun ./scripts/prepare.ts
        working-directory: packages/desktop
        env:
          OPENCODE_VERSION: ${{ needs.version.outputs.version }}
          OPENCODE_CHANNEL: prod
          RUST_TARGET: ${{ matrix.settings.target }}

      - name: Build
        run: bun run build
        working-directory: packages/desktop
        env:
          NODE_OPTIONS: --max-old-space-size=4096
          OPENCODE_VERSION: ${{ needs.version.outputs.version }}
          OPENCODE_CHANNEL: prod

      - name: Package
        run: npx electron-builder ${{ matrix.settings.platform_flag }} --publish never --config electron-builder.config.ts
        working-directory: packages/desktop
        timeout-minutes: 60
        env:
          OPENCODE_CHANNEL: prod

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.settings.artifact }}
          path: packages/desktop/dist/*
          if-no-files-found: error
