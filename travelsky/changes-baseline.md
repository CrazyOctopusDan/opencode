# TravelSky 需求说明修改文件（冲突基线）

> 用途：后续与上游合并冲突时，以本文件记录的改动点为保留基线。

## 变更清单

| 文件                                                                        | 类型 | 改动目的                                                                           | 关键改动点                                                                                                                                             | 冲突保留策略                                                                                                            |
| --------------------------------------------------------------------------- | ---- | ---------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------- |
| `packages/opencode/src/cli/travelsky/auth-store.ts`                         | 新增 | 本地 token 持久化与有效期判断                                                      | `read/current/write/clear`；文件 `travelsky-auth.json`                                                                                                 | 保留全部；如上游有同类实现，优先保留 `expires_at` 语义与 0600 权限写入                                                  |
| `packages/opencode/src/cli/travelsky/login.ts`                              | 新增 | CLI 登录交互与 `/global/login` 调用                                                | `request`（协议不变）、`prompt`（Retry/Exit）                                                                                                          | 保留接口 URL 与请求体字段原样；冲突时不引入后端字段改动                                                                 |
| `packages/opencode/src/cli/travelsky/header.ts`                             | 新增 | Bearer 头构造                                                                      | `bearer/headers`                                                                                                                                       | 保留全部                                                                                                                |
| `packages/opencode/src/cli/travelsky/provider.ts`                           | 新增 | TravelSky provider 识别统一                                                        | `isTravelSky()` 大小写兼容                                                                                                                             | 保留大小写不敏感判断                                                                                                    |
| `packages/opencode/src/cli/travelsky/bootstrap.ts`                          | 新增 | 启动登录门禁与 provider 可用性预检                                                 | `ensureLogin`；预检 `/provider` + `/config/providers`；旧 token 被本地服务拒绝时清理并重新登录                                                         | 保留“先复用 token，失败再登录”的顺序；保留接口不变；不能把本地 401 直接带入 TUI                                        |
| `packages/opencode/src/cli/cmd/tui/thread.ts`                               | 修改 | `opencode` 启动前执行登录门禁并注入 Bearer                                         | 调用 `ensureLogin`；`validateSession()` 在登录后执行并携带同一个 Authorization；`tui({ headers })` 注入 Authorization                                  | 冲突时必须保留 `ensureLogin` 在 `validateSession()` 和 `tui(...)` 前执行，避免带 `--session` 启动时先 401              |
| `packages/opencode/src/cli/cmd/tui/app.tsx`                                 | 修改 | `/connect` 与 `/models` 只走 TravelSky                                             | `travel` memo；`DialogModel providerID={travel()}`                                                                                                     | 冲突时保留 `/connect` 与 `/models` 的 provider 固定逻辑                                                                 |
| `packages/opencode/src/cli/cmd/tui/component/dialog-provider.tsx`           | 修改 | provider 弹窗仅显示/处理 TravelSky                                                 | 过滤 `provider_next.all` 到 TravelSky                                                                                                                  | 冲突时保留过滤逻辑，不回退到全 provider 展示                                                                            |
| `packages/opencode/src/server/tempo-api.ts`                                 | 修改 | 复用 Tempo 主机解析给 metric 上报，并识别 Tempo token 失效                         | 新增 `TempoApi.baseURL()`；`TempoApi.expired()` 识别 `success=false/code=401 或 "401"/message` 前缀；`listModels()` 返回 `ok/expired/unavailable` 状态 | 冲突时保留 `baseURL()` 导出和 expired 状态，避免模型列表把 token 失效吞成普通空列表                                     |
| `packages/opencode/src/provider/model-policy.ts`                            | 修改 | 将 Tempo token 失效暴露给 provider 列表，同时避免污染模型缓存和放开锁定            | `snapshot()` 保存 `expired/expiredMessage`；强制刷新遇到 expired 不覆盖既有全局 policy；有本地 token 时 expired 仍保持 `locked=true`                   | 冲突时保留 expired 为瞬时锁定状态，不允许过期响应清空缓存或临时放开非 TravelSky provider                                |
| `packages/opencode/src/provider/provider.ts`                                | 修改 | provider 响应 schema 暴露 Tempo 调试状态，并在 Tempo expired 时继续锁住 provider   | `Provider` 增加可选 `debug_tempo.auth_expired/message`；`policy.expired` 时过滤 config providers，并让 `isProviderAllowed()` 返回 false                | 冲突时保留字段名、布尔语义和 expired 不放行任何非 TravelSky provider 的规则                                             |
| `packages/opencode/src/server/routes/instance/provider.ts`                  | 修改 | 旧 provider 路由透传 Tempo token 失效状态，并避免 expired 时暴露普通 provider      | provider list 响应在 expired 时附带 `debug_tempo`，同时 `all/connected` 为空                                                                           | 冲突时保留旧路由输出和 expired 锁定，避免 Desktop 源继续使用旧 SDK 时无法恢复或看到非 TravelSky provider                |
| `packages/opencode/src/server/routes/instance/httpapi/handlers/provider.ts` | 修改 | HTTPAPI provider 路由透传 Tempo token 失效状态，并避免 expired 时暴露普通 provider | provider list 响应在 expired 时附带 `debug_tempo`，同时 `all/connected` 为空                                                                           | 冲突时保留 HTTPAPI 输出和 expired 锁定，确保新旧客户端路径行为一致                                                      |
| `packages/opencode/src/server/tempo-metric.ts`                              | 新增 | 发送回答统计到公司后端，遇到 Tempo token 失效时静默跳过                            | `POST /ai/data/api/open/code/metric/add`；复用 `TempoSession` cookie；解析响应，expired、非 2xx、`success=false` 返回 `false`                          | 保留全部；若上游有同名能力，优先保留“同源主机+cookie+超时容错+失效不打扰用户”                                           |
| `packages/opencode/src/session/metric.ts`                                   | 新增 | 聚合回答字数与 TravelSky `other.v1/v2` 统计矩阵                                    | 同 `parentID` 多 step assistant 聚合；`other.v1` 记录文件改动矩阵，优先用 snapshot diff，并合并 snapshot 未覆盖的工具 metadata 文件，空 diff 时从完成工具 metadata 兜底；`other.v2` 记录用户/Agent/工具/中断矩阵；不再写入 token 与回答代码块旧口径 | 保留全部；冲突时保留 `other.v1` 与 `other.v2` 双版本结构，项目外文件也要进入 v1，文件改动只上传聚合信息，工具统计按 tool part 与 step 事件计算 |
| `packages/opencode/src/session/prompt.ts`                                   | 修改 | 在完整回答后触发单次上报并计算文件变更                                             | `runLoop` 结束点基于本轮 `step-start/step-finish` snapshot 调用 `Snapshot.diffFull`，再调用 `SessionMetric.build` + `TempoMetric.send`                 | 冲突时保留“完成态一次上报”时机，避免多次重复上报；保留完成后再取 diff 的顺序                                            |
| `packages/opencode/src/snapshot/index.ts`                                   | 修改 | 非 git 目录也能为 `other.v1` 生成最终文件 diff                                     | 非 git 项目使用当前目录作为内部 snapshot worktree；不再因为 `project.vcs` 为空禁用 snapshot                                                           | 冲突时保留“非 git 可 snapshot，但不把项目伪装成 git VCS”的语义；禁止把非 git 对比根设为 `/`                            |
| `packages/opencode/src/tool/write.ts`                                       | 修改 | 为 `write` 工具补充文件变更 metadata，支撑 `other.v1` 兜底统计                    | 返回 `metadata.filediff.file/status/additions/deletions/patch`；创建文件标记 `added`，覆盖文件标记 `modified`                                        | 冲突时保留 `filediff` 结构或等价字段，避免 `write` 创建/覆盖文件时 v1 文件矩阵归零                                      |
| `packages/opencode/test/cli/tui/thread.test.ts`                             | 修改 | 适配登录门禁                                                                       | mock `ensureLogin` + 断言 `Authorization: Bearer ...` 已注入 `tui`，并断言显式 session 校验携带同一 Bearer                                             | 保留 mock、header 注入和 `validateSession` 带 auth 断言，防止 CLI 带 `--session` 回归为先校验再登录                   |
| `packages/opencode/test/cli/travelsky-auth-store.test.ts`                   | 新增 | token 存储回归                                                                     | 读写与过期场景                                                                                                                                         | 保留全部                                                                                                                |
| `packages/opencode/test/cli/travelsky-login.test.ts`                        | 新增 | 登录接口调用回归                                                                   | 成功/失败消息 + `prompt` 的 retry/exit/非TTY 场景                                                                                                      | 保留全部                                                                                                                |
| `packages/opencode/test/cli/travelsky-bootstrap.test.ts`                    | 新增 | 登录门禁回归                                                                       | 复用 token、缺 token 登录、旧 token 被本地服务拒绝后重新登录场景                                                                                       | 保留全部，尤其保留旧 token 401 后重新登录断言                                                                           |
| `packages/opencode/test/server/tempo-api.test.ts`                           | 修改 | Tempo 模型列表与 token 失效回归                                                    | 覆盖公司 `success=false/code=401/message` 前缀格式、字符串 `"401"` 兼容和 `listModels()` expired 状态                                                  | 保留全部，防止失效响应回退为普通空列表                                                                                  |
| `packages/opencode/test/provider/model-policy.test.ts`                      | 修改 | provider policy token 失效回归                                                     | 覆盖 expired 状态透传，以及过期响应不覆盖既有 policy 缓存                                                                                              | 保留全部，防止模型缓存被失效响应污染                                                                                    |
| `packages/opencode/test/server/tempo-metric.test.ts`                        | 新增 | metric 请求发送与失效静默跳过回归                                                  | URL、Cookie、请求体、无登录态跳过、expired/失败响应返回 `false`                                                                                        | 保留全部                                                                                                                |
| `packages/opencode/test/session/metric.test.ts`                             | 新增 | `other.v1/v2` 统计聚合口径回归                                                     | 覆盖回答字数、provider 过滤、v1 文件增删改/语言分布/重复修改、项目外工具文件合并、工具 metadata 兜底、v2 用户消息/Agent 回复/step/工具成功率/失败码/结束类型 | 保留全部                                                                                                                |
| `packages/opencode/test/snapshot/snapshot.test.ts`                          | 修改 | 非 git 目录 snapshot diff 回归                                                     | 覆盖无源 git 仓库目录中新增文件后 `diffFull` 返回 `added/additions`                                                                                    | 保留该测试，防止 v1 在非 git 目录重新归零                                                                               |
| `packages/opencode/test/tool/write.test.ts`                                 | 修改 | `write` 工具文件变更 metadata 回归                                                  | 覆盖 `metadata.filediff` 中的文件路径、状态、增删行和 patch 字段                                                                                       | 保留 `filediff` 断言，防止 v1 兜底统计失去 write 来源                                                                   |
| `packages/desktop/src/preload/types.ts`                                     | 修改 | Desktop preload 类型暴露安全凭据 IPC                                               | `secureCredentialGet/Set/Delete` 类型；返回 `passwordAvailable/passwordSaved` 状态                                                                     | 冲突时保留 API 名称和“不保证密码可用”的返回语义                                                                         |
| `packages/desktop/src/preload/index.ts`                                     | 修改 | Desktop preload 暴露安全凭据 IPC                                                   | 通过 `ipcRenderer.invoke("secure-credential-*")` 转发                                                                                                  | 冲突时保留只通过 preload 暴露受限安全凭据入口                                                                           |
| `packages/desktop/src/main/ipc.ts`                                          | 修改 | Desktop 原生层加密保存记住我凭据                                                   | `safeStorage` 可用时保存 base64 密文；不可用或解密失败时只返回用户名和 `passwordAvailable=false`                                                       | 冲突时禁止明文保存密码；保留加密不可用时用户名可填、密码不可恢复的降级                                                  |
| `packages/app/src/travelsky/auth.ts`                                        | 新增 | TravelSky Desktop 登录辅助层                                                       | 封装已记住凭据的读取/保存/清理，以及 `debug_tempo.auth_expired` 识别                                                                                   | 保留该二开隔离层，避免公司 token 失效逻辑扩散到通用 SDK/页面                                                            |
| `packages/app/src/context/auth.tsx`                                         | 修改 | 登录态恢复与记住我保存                                                             | `login(username,password,remember)`；`recover()` 使用已记住凭据静默重登；`authOperation` 防止竞态清空新登录态                                          | 冲突时保留“无可用密码则清理并回登录”的语义，以及竞态保护                                                                |
| `packages/app/src/utils/server.ts`                                          | 修改 | Desktop SDK 本地 401 自动恢复                                                      | `createAuthRecoveringFetch()` 每次 SDK 请求前注入最新 Bearer；本地 sidecar 401 后调用 `auth.recover()`，成功后用新 token 重试原请求一次                  | 冲突时保留 fetch 层恢复，避免 sidecar 重启后发送消息、事件流或目录请求直接 401；不得无限重试                             |
| `packages/app/src/context/global-sdk.tsx`                                   | 修改 | 全局 SDK 请求接入登录恢复                                                          | 主 SDK、`createClient()` 目录 SDK 和 event SDK 统一使用 auth recovering fetch，覆盖 `/session/.../prompt_async`、event stream、provider/file/workspace 请求 | 冲突时保留所有 SDK 创建路径的 recovering fetch；不能只修模型列表，否则聊天发送仍会 401                                  |
| `packages/app/src/context/global-sync/bootstrap.ts`                         | 修改 | 全局和目录 provider 查询遇到 Tempo expired 时统一恢复登录并重试                    | `listProvidersWithRecovery()` 识别 `debug_tempo.auth_expired` 与 unauthorized；恢复成功后用新 client 重试；恢复失败返回空 provider list                | 冲突时保留该统一入口，避免只在模型弹窗恢复而全局缓存仍旧过期                                                            |
| `packages/app/src/context/global-sync.tsx`                                  | 修改 | 全局 provider 启动加载接入 Desktop 登录恢复                                        | `recoverProviderAuth()` 调用 `auth.recover()` 并创建带新 token 的全局或目录 client                                                                     | 冲突时保留 GlobalSync 启动期恢复，否则重启后仍会停留操作页但模型缓存为空                                                |
| `packages/app/src/context/global-sync/child-store.ts`                       | 修改 | 目录级 provider query 接入登录恢复                                                 | child store 的 `loadProvidersQuery` 传入目录级 `recoverProviderAuth`                                                                                   | 冲突时保留目录 provider cache 恢复，保证 `useProviders()` 和 `useModels()` 看到新模型                                   |
| `packages/app/src/pages/login.tsx`                                          | 修改 | 登录页增加“记住我”并回填凭据                                                       | 加载已记住凭据；有可用密码才默认勾选；用户编辑后不被异步回填覆盖                                                                                       | 冲突时保留 checkbox 文案和回填保护，避免覆盖用户正在输入的表单                                                          |
| `packages/app/src/components/dialog-select-model.tsx`                       | 修改 | 模型选择弹窗遇到 Tempo token 失效时恢复登录并刷新目录 provider cache               | 通过 `loadProvidersQuery(sdk.directory, sdk.client)` 复用统一恢复逻辑；恢复成功后 query cache 写入新模型，否则 `logout+navigate("/login")`             | 冲突时保留目录 query cache 刷新，避免弹窗可选但提交仍提示 model required                                                |
| `packages/app/src/components/dialog-select-model-unpaid.tsx`                | 修改 | 未付费模型选择弹窗同步登录恢复并刷新目录 provider cache                            | 与主模型弹窗保持同样的 query cache/recover/retry/logout 语义                                                                                           | 冲突时两个模型弹窗必须语义一致                                                                                          |
| `packages/app/src/travelsky/auth.test.ts`                                   | 新增 | TravelSky 登录辅助层回归                                                           | 仅 Desktop、IPC 缺失、凭据读取保存清理、expired 识别                                                                                                   | 保留全部                                                                                                                |
| `packages/app/src/context/auth.test.tsx`                                    | 新增 | auth context 记住我与恢复回归                                                      | 勾选保存、未勾选清理、无凭据/失败清理、竞态恢复不覆盖新登录                                                                                            | 保留全部                                                                                                                |
| `packages/app/src/utils/server.test.ts`                                     | 修改 | SDK fetch 本地 401 恢复回归                                                        | 覆盖 401 后 recover 并重试一次、recover 失败不重试、首包使用最新 token 覆盖旧 Authorization                                                            | 保留这些断言，防止 Desktop 重启后进入对话页但发送消息 401 的问题回归                                                    |
| `packages/app/src/context/global-sync.test.ts`                              | 修改 | provider list 恢复回归                                                             | expired 时 recover 后重试，recover 失败时返回空 provider list                                                                                          | 保留全部，防止全局/目录缓存恢复链路回归                                                                                 |
| `packages/app/src/pages/login.test.ts`                                      | 新增 | 登录页记住我回填回归                                                               | remembered 表单推导、异步回填不覆盖用户编辑                                                                                                            | 保留全部                                                                                                                |
| `packages/sdk/openapi.json`                                                 | 生成 | provider list schema 包含 Tempo 失效调试字段                                       | `debug_tempo.auth_expired/message`                                                                                                                     | 随 schema 生成同步更新；冲突时以 server schema 为准重新生成                                                             |
| `packages/sdk/js/src/v2/gen/types.gen.ts`                                   | 生成 | JS SDK 类型包含 Tempo 失效调试字段                                                 | provider 类型增加 `debug_tempo`                                                                                                                        | 随 SDK 生成同步更新；冲突时运行 `./packages/sdk/js/script/build.ts`                                                     |
| `travelsky/design.md`                                                       | 新增 | 需求实现文档                                                                       | Desktop 对齐、CLI 流程、边界说明                                                                                                                       | 持续更新                                                                                                                |
| `travelsky/metric-design.md`                                                | 新增 | metric 需求实现文档                                                                | 上报时机、`other.v1/v2` 统计口径、非 git 内部 snapshot、snapshot diff 与工具 metadata 兜底、文件落点                                                    | 持续更新                                                                                                                |
| `travelsky/metric-CONTEXT.md`                                               | 新增 | 需求决策上下文                                                                     | phase 边界、决策、`v1` 文件矩阵、非 git 内部 snapshot、工具 metadata 兜底、`v2` 会话/工具矩阵、冲突参考                                                 | 持续更新                                                                                                                |
| `travelsky/desktop-auth-design.md`                                          | 新增 | Desktop 登录态恢复与记住我设计文档                                                 | 根因、目标、文件落点、数据流、验证计划                                                                                                                 | 持续更新                                                                                                                |
| `travelsky/desktop-auth-implementation-plan.md`                             | 新增 | Desktop 登录态恢复执行计划                                                         | 分任务实施计划、验证点、风险边界                                                                                                                       | 持续更新                                                                                                                |
| `travelsky/baseline-checklists/desktop-auth.md`                             | 新增 | Desktop 登录态恢复合并保护清单                                                     | 保护目标、不变量、冲突处理规则、验证方式                                                                                                               | 后续合并时按清单逐项核对                                                                                                |
| `travelsky/baseline-checklists/metric-reporting.md`                         | 新增 | TravelSky metric 上报统计矩阵合并保护清单                                          | `other.v1/v2` 字段、非 git 内部 snapshot、snapshot diff 与工具 metadata 兜底、不变量、冲突处理规则、验证方式                                            | 后续合并时按清单逐项核对                                                                                                |
| `travelsky/changes-baseline.md`                                             | 新增 | 合并冲突基线                                                                       | 本清单                                                                                                                                                 | 持续更新                                                                                                                |

## 合并冲突处理建议

- 先按上表定位文件，再按“关键改动点”逐项核对，不按整文件覆盖。
- 若上游也新增登录链路：保留“接口不变 + CLI 本地 token + TravelSky 单 provider”的约束。
- CLI 登录门禁必须先于受保护请求：旧 token 先预检，失败即清理并重新登录；带 `--session` 时 session 校验必须携带新 Bearer。
- 若上游调整 TUI 命令注册：优先保留 `/connect` 与 `/models` 的 TravelSky 固定入口行为。
- 若上游新增 metric 统计：优先保留“runLoop 完成态单次上报 + `other.v1` 文件改动矩阵 + `other.v2` 会话/工具矩阵”的语义。
- 文件修改统计只上传聚合信息，不上传 diff 原文；`other.v1` 必须覆盖修改文件数、新增文件数、删除文件数、增删行、净增行、语言分布、单文件最大修改行数、重复修改文件数。
- 非 git 目录也必须能统计 `other.v1`：内部 snapshot 对比根限定为当前目录，不能因为没有 `.git` 返回全 0，也不能把对比范围扩大到 `/`。
- 若上游调整 Desktop 登录或 provider SDK：优先保留 `debug_tempo.auth_expired` 触发的模型列表恢复链路；恢复成功后必须使用新建 client 重试，恢复失败必须清理登录态并回到登录页。
- 若上游调整全局 SDK、fetch 注入或 event stream：必须保留本地 sidecar 401 后 `auth.recover()` 并重试原请求一次的能力，尤其是 `/session/.../prompt_async` 发送消息链路。
- 若上游调整 Desktop 存储：记住我密码只能通过 Electron `safeStorage` 密文保存；加密不可用、解密失败或 IPC 不可用时只能填用户名，不能保存或恢复明文密码。

## Desktop 对齐来源

- `packages/app/src/context/auth.tsx`：Desktop 登录与 token 过期判断
- `packages/app/src/utils/server.ts`：Desktop Bearer 头注入与本地 401 自动恢复
- `packages/app/src/context/global-sdk.tsx`：Desktop 全局 SDK、目录 SDK 和事件流 SDK 的 fetch 接入
- `packages/app/src/components/dialog-select-model.tsx`：Desktop 从 `provider.list()` 构建模型列表
- `packages/opencode/src/server/routes/global.ts`、`provider.ts`、`config.ts`：后端接口定义（本次不改）
